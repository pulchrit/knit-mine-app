export default {
    //API_ENDPOINT: 'http://localhost:8000/',
    API_ENDPOINT: 'https://quiet-shelf-50620.herokuapp.com/',
    TOKEN_KEY: 'AuthToken',
  }

  // Storing the below here in case I need to replace the other package.json deploy script
  //"deploy": "now --prod ./build && now rm knit-mine-app --safe --yes"
  